name: Daily Commit

on:
  schedule:
    - cron: '0 6 * * *'   # 11:30 AM IST
    - cron: '0 18 * * *'  # 11:30 PM IST
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Make a commit
        run: |
          echo "$(date)" >> daily-log.txt
          git config user.name "DipanshuA"
          git config user.email "silentaddy6@gmail.com"
          git add daily-log.txt
          git commit -m "Daily update" || echo "No changes to commit"

      - name: Push changes
        uses: ad-m/github-push-action@v0.8.0
        with:
          github_token: ${{ secrets.PERSONAL_TOKEN }}
